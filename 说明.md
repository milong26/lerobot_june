这个代码搞下来只是为了检查将3.0的数据集变成2.1对不对。
安装完必要的，
运行check_dataset.py即可。

最后卸载使用的lb2_1环境：conda remove --name lb2_1 --all
git remote set-url origin  https://github.com/milong26/lerobot_june.git
git add .
git commit -m "测试30->21转换是否成功，只是测试，不是主代码"
因为之前已经执行国前面两个，并且commit id是bdc05a3c
git switch -c backup-v0.3.3-0720 bdc05a3c
git push -u origin backup-v0.3.3-0720