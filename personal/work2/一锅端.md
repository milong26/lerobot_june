# 用一个新的来验证
## collect dataset
still metaworld

python personal/work2/collect_dataset/metaworld/collect_metaworld_bytask.py \
    --task coffee-push-v3 \
    --num-random-episodes 300 \
    --num-uniform-episodes 100 \
    --output-dir personal/work2/dataset_view/coffee_push_corner3/ \
    --repo-id work2/coffee_push_corner3 \
    --seed-start 0 \
    --streaming-encoding \
    --encoder-threads 4

再找个任务
python personal/work2/collect_dataset/metaworld/collect_metaworld_bytask.py \
    --task shelf-place-v3 \
    --num-random-episodes 300 \
    --num-uniform-episodes 100 \
    --output-dir personal/work2/dataset_view/shelf_place_corner/ \
    --repo-id work2/shelf_place_corner \
    --seed-start 0 \
    --streaming-encoding \
    --encoder-threads 4

修改了代码，准备重新采集数据
记得检查代码是不是corner
python personal/work2/collect_dataset/metaworld/collect_metaworld_bytask.py \
    --task disassemble-v3 \
    --num-random-episodes 300 \
    --num-uniform-episodes 100 \
    --output-dir personal/work2/dataset_view/disassemble-v3_corner/ \
    --repo-id work2/disassemble-v3_corner \
    --seed-start 0 \
    --streaming-encoding \
    --encoder-threads 8
运行完以后查看日志：
18:13:18 重新开始采集



## 开启