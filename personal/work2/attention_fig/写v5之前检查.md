2026年9月3日15:12:38 改完attentionfig以后准备检查看

1. 运行 inference_trace 在gpu=0上面运行
nohup bash -c '
export CUDA_VISIBLE_DEVICES=0
export MUJOCO_GL=egl
export PYOPENGL_PLATFORM=egl
python personal/work2/attention_fig/plot_attention.py \
    --mode inference_trace \
    --model-set corner_12k \
    --seeds 10042,10043,10044,10045,10046,10047,10048,10049,10050,10051 \
    --layers 3,7,11 \
    --query-mode mean_suffix \
    --average-heads \
    --trace-heatmap-steps first,mid,last \
    --sanity-check
' > personal/work2/attention_fig/log_inference_trace_12k_corner.log 2>&1 &
过会儿重新运行？

2. 运行 rollout attention analysis
nohup bash -c '
export CUDA_VISIBLE_DEVICES=0
export MUJOCO_GL=egl
export PYOPENGL_PLATFORM=egl
python personal/work2/attention_fig/plot_attention.py \
    --mode rollout \
    --model-set corner_12k \
    --seeds 10042,10043,10044,10045,10046,10047,10048,10049,10050,10051 \
    --layers 3,7,11 \
    --query-mode mean_suffix \
    --average-heads \
    --trace-heatmap-steps first,mid,last \
    --sanity-check
' > personal/work2/attention_fig/log_rollout_12k_corner.log 2>&1 &




---前面两个成功以后---
3. aggregate the results
nohup python personal/work2/attention_fig/aggregate_12k_corner.py \
    > personal/work2/attention_fig/log_aggregate_12k_corner.log 2>&1 &

