执行过
nohup env CUDA_VISIBLE_DEVICES=0 lerobot-train   --policy.type=act   --dataset.repo_id=lerobot/metaworld_mt50   --env.type=metaworld   --env.task=pick-place-v3   --output_dir=outputs/act_mw_pick-place-v3   --job_name=act_mt_pick-place-v3   --policy.device=cuda   --steps=20000   --wandb.enable=true   > train_log/mt_act.log 2>&1 &
预计2026年7月22日0点完成，然后需要测试。
2026年7月22日10:17:21 准备测试
nohup env \
MUJOCO_GL=egl \
PYOPENGL_PLATFORM=egl \
CUDA_VISIBLE_DEVICES=1 \
lerobot-eval \
  --policy.path=outputs/act_mw_pick-place-v3/checkpoints/020000/pretrained_model \
  --env.type=metaworld \
  --env.task=pick-place-v3 \
  --eval.n_episodes=50 \
  --eval.batch_size=1 \
  --eval.save_dataset=True \
> train_log/mt_act_eval.log 2>&1 &




2026年7月21日21:18:59 开始看metaworld数据集的内容物和能否扩充

- `personal/test/metaworld/test_pick_place_dataset.py` - 检查数据集元数据、features、tasks、episodes 结构，对比数据集和原始环境的 observation
- `personal/test/metaworld/test_env_state.py` - 检查 observation.environment_state 的内容，解析 39 维 observation 的结构（手臂位置、物体位置、目标位置等）
- `personal/test/metaworld/extract_init_positions.py` - 提取所有 pick-place-v3 episode 的初始物体位置，统计分析分布情况
- `personal/test/metaworld/check_workspace_limits.py` - 检查 Meta-World 环境的工作空间限制、goal_space、机械臂可达范围
- `personal/test/metaworld/generate_metaworld_dataset.py` - 使用 expert policy 自动生成更多 pick-place-v3 示范数据，支持随机化物体位置和目标位置
- `personal/test/metaworld/readme.md` - 记录分析结论、代码示例、如何拓展数据集的说明

结论：
1. 数据集包含 2500 个 episode（50 个任务 × 50 个 episode/任务）
2. observation.environment_state 是 39 维，包含手臂位置、物体位置、目标位置等信息
3. 可以从数据集提取每个 episode 的初始物体位置：`frame[0]['observation.environment_state'][4:7]`
4. 50 个 pick-place-v3 episode 使用不同 seed 随机化物体位置，目标位置固定为 [0.1, 0.8, 0.2]
5. 数据集可以拓展，使用 expert policy 可以生成任意数量的示范数据



2026年7月23日15:04:07开始train，使用commit id=bbb84d8abda0ac5b493468458fdbd0f20a92111c的代码+下面的指令训练。
nohup env \
  HF_LEROBOT_HOME=/data/zhonglinye/hfdata/ \
  CUDA_VISIBLE_DEVICES=2,3 \
  MUJOCO_GL=egl \
accelerate launch \
  --config_file=accelerate_tmp/accelerate_config.yaml \
  $(which lerobot-train) \
  --policy.type=act \
  --dataset.repo_id=lerobot/metaworld_mt50 \
  --dataset.episodes="[1500, 1501, 1502, 1503, 1504, 1505, 1506, 1507, 1508, 1509, 1510, 1511, 1512, 1513, 1514, 1515, 1516, 1517, 1518, 1519, 1520, 1521, 1522, 1523, 1524, 1525, 1526, 1527, 1528, 1529, 1530, 1531, 1532, 1533, 1534, 1535, 1536, 1537, 1538, 1539, 1540, 1541, 1542, 1543, 1544, 1545, 1546, 1547, 1548, 1549]" \
  --env.type=metaworld \
  --env.task=pick-place-v3 \
  --output_dir=outputs/act_mw_pick-place-v3_multigpu \
  --job_name=act_mt_pick-place-v3_multigpu \
  --policy.device=cuda \
  --steps=20000 \
  --eval_freq=2000 \
  --wandb.enable=true \
  --remove_features='["observation.environment_state"]' \
  --eval.n_episodes=50 \
  > train_log/mt_act_multigpu.log 2>&1 &
预计2026年7月23日15:12:15+5h=20点结束，到时候看能否测试，模型效果怎么样。
TODO: 等待测试。测试代码还没有确认，等训练完成再确认吧



2026年7月23日15:24:01开始训练分辨率减半+smolvla+ep10/all数据集

nohup env \
  LD_LIBRARY_PATH=$CONDA_PREFIX/lib:$LD_LIBRARY_PATH \
  CUDA_VISIBLE_DEVICES=1,3 \
  HF_HUB_OFFLINE=1 \
accelerate launch \
  --main_process_port=0 \
  --config_file=accelerate_tmp/accelerate_config.yaml \
  $(which lerobot-train) \
  --dataset.repo_id=ep10/all \
  --dataset.root=personal/work1/ep10_all \
  --policy.path=lerobot/smolvla_base \
  --output_dir=outputs/smolvla_ep10_half \
  --job_name=smolvla_ep10_half \
  --policy.device=cuda \
  --policy.push_to_hub=false \
  --wandb.enable=true \
  --steps=26000 \
  --batch_size=32 \
  --rename_map='{"observation.images.side":"observation.images.camera1","observation.images.wrist":"observation.images.camera2"}' \
  --half_img_resolu=true \
  > train_log/test_smolvla_ep10.log 2>&1 &