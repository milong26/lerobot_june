



2026年7月21日21:18:59 开始看metaworld数据集的内容物和能否扩充

- `personal/test/metaworld/test_pick_place_dataset.py` - 检查数据集元数据、features、tasks、episodes 结构，对比数据集和原始环境的 observation
- `personal/test/metaworld/test_env_state.py` - 检查 observation.environment_state 的内容，解析 39 维 observation 的结构（手臂位置、物体位置、目标位置等）
- `personal/test/metaworld/extract_init_positions.py` - 提取所有 pick-place-v3 episode 的初始物体位置，统计分析分布情况
- `personal/test/metaworld/check_workspace_limits.py` - 检查 Meta-World 环境的工作空间限制、goal_space、机械臂可达范围
- `personal/test/metaworld/generate_metaworld_dataset.py` - 使用 expert policy 自动生成更多 pick-place-v3 示范数据，支持随机化物体位置和目标位置
- `personal/test/metaworld/readme.md` - 记录分析结论、代码示例、如何拓展数据集的说明

结论：
1. 数据集包含 2500 个 episode（50 个任务 × 50 个 episode/任务）
2. observation.environment_state 是 39 维，包含手臂位置、物体位置、目标位置等信息
3. 可以从数据集提取每个 episode 的初始物体位置：`frame[0]['observation.environment_state'][4:7]`
4. 50 个 pick-place-v3 episode 使用不同 seed 随机化物体位置，目标位置固定为 [0.1, 0.8, 0.2]
5. 数据集可以拓展，使用 expert policy 可以生成任意数量的示范数据



2026年7月24日10:58:23开始train，使用commit id=3b4bcd82b7d3748be1a436c4e8322b8d5eab39be 的代码+下面的指令训练。
tmux new -s act_train
train_log/mt_act_multigpu.log
预计2026年7月24日10:58:51+5h=15点结束，到时候看能否测试，模型效果怎么样。
TODO: 等待测试。测试代码还没有确认，等训练完成再确认吧




2026年7月23日15:24:01开始训练分辨率减半+smolvla+ep10/all数据集，使用commit id=3b4bcd82b7d3748be1a436c4e8322b8d5eab39be
使用下面的指令

tmux new -s smolvla_ep10_half
conda activate lb_server

env \
  LD_LIBRARY_PATH=$CONDA_PREFIX/lib:$LD_LIBRARY_PATH \
  CUDA_VISIBLE_DEVICES=1,2 \
  HF_HUB_OFFLINE=1 \
accelerate launch \
  --main_process_port=0 \
  --config_file=accelerate_tmp/accelerate_config.yaml \
  $(which lerobot-train) \
  --dataset.repo_id=ep10/all \
  --dataset.root=personal/work1/ep10_all \
  --policy.path=lerobot/smolvla_base \
  --output_dir=outputs/smolvla_ep10_half \
  --job_name=smolvla_ep10_half \
  --policy.device=cuda \
  --policy.push_to_hub=false \
  --wandb.enable=true \
  --steps=26000 \
  --batch_size=32 \
  --rename_map='{"observation.images.side":"observation.images.camera1","observation.images.wrist":"observation.images.camera2"}' \
  --half_img_resolu=true \
  > train_log/test_smolvla_ep10.log 2>&1

预计3小时完成15:30:39+3h=18点半
TODO: 等待测试。测试代码需要修改robot_client，且需要注限位。


训练tinyvla+ep10，一开始设置了batch_size=32后cuda out of
memeory，一点点降低到4。

chunk_size必须是2的倍数。


tmux new -s tinyvla_ep10_half
conda activate lb_server

env \
  LD_LIBRARY_PATH=$CONDA_PREFIX/lib:$LD_LIBRARY_PATH \
  CUDA_VISIBLE_DEVICES=0,1,2 \
  HF_HUB_OFFLINE=1 \
accelerate launch \
  --main_process_port=0 \
  --config_file=accelerate_tmp/accelerate_config.yaml \
  $(which lerobot-train) \
  --dataset.repo_id=ep10/all \
  --policy.type=tinyvla \
  --policy.action_head_type=droid_diffusion \
  --dataset.root=personal/work1/ep10_all \
  --policy.model_name_or_path=lesjie/Llava-Pythia-400M \
  --output_dir=outputs/tinyvla_ep10_half_50step \
  --job_name=tinyvla_ep10_half_50step \
  --policy.device=cuda \
  --policy.chunk_size=32 \
  --policy.n_action_steps=32 \
  --policy.lora_enable=true \
  --policy.push_to_hub=false \
  --wandb.enable=true \
  --steps=26000 \
  --batch_size=8 \
  --num_workers=4 \
  --half_img_resolu=true \
  --policy.freeze_vision_tower=true \
  > train_log/test_tinyvla_ep10_50step.log 2>&1

TODO: 不知道能不能正常运行，大概需要4h。2026年7月29日15:34:39+4h=19点半



训练evo1+ep10/all数据集+half分辨率

再训练一个
env \
  LD_LIBRARY_PATH=$CONDA_PREFIX/lib:$LD_LIBRARY_PATH \
  CUDA_VISIBLE_DEVICES=0,1,2 \
  HF_HUB_OFFLINE=1 \
accelerate launch \
  --main_process_port=0 \
  --config_file=accelerate_tmp/accelerate_config.yaml \
  $(which lerobot-train) \
  --dataset.repo_id=ep10/all \
  --policy.type=tinyvla \
  --policy.action_head_type=droid_diffusion \
  --dataset.root=personal/work1/ep10_all \
  --policy.model_name_or_path=lesjie/Llava-Pythia-400M \
  --output_dir=outputs/tinyvla_ep10_half_16step \
  --job_name=tinyvla_ep10_half_16step \
  --policy.device=cuda \
  --policy.chunk_size=64 \
  --policy.n_action_steps=20 \
  --policy.lora_enable=true \
  --policy.push_to_hub=false \
  --wandb.enable=true \
  --steps=26000 \
  --batch_size=8 \
  --num_workers=4 \
  --half_img_resolu=true \
  --policy.freeze_vision_tower=true \
  > train_log/test_tinyvla_ep10_64step.log 2>&1


训练smovla+ur5+第一个任务—+pickup bushing
/data/zhonglinye/jun/ur5_lerobot_dataset/ur5_lerobot_dataset
tmux new -s smolvla_ur5_first
conda activate lb_server

env \
  LD_LIBRARY_PATH=$CONDA_PREFIX/lib:$LD_LIBRARY_PATH \
  CUDA_VISIBLE_DEVICES=0,1,2 \
  HF_HUB_OFFLINE=1 \
accelerate launch \
  --main_process_port=0 \
  --config_file=accelerate_tmp/accelerate_config.yaml \
  $(which lerobot-train) \
  --dataset.repo_id=ur5_lerobot_dataset/ur5_lerobot_dataset \
  --dataset.root=/data/zhonglinye/jun/ur5_lerobot_dataset/ur5_lerobot_dataset \
  --policy.path=lerobot/smolvla_base \
  --output_dir=outputs/smolvla_ur5_first \
  --job_name=smolvla_ur5_first \
  --policy.device=cuda \
  --policy.push_to_hub=false \
  --wandb.enable=true \
  --steps=30000 \
  --batch_size=32 \
  --rename_map='{"observation.images.base":"observation.images.camera1","observation.images.wrist":"observation.images.camera2"}' \
  --half_img_resolu=false \
  > train_log/test_smolvla_ur5_first.log 2>&1



lerobot-train \
  --dataset.repo_id=ur5_lerobot_dataset/ur5_lerobot_dataset \
  --dataset.root=/data/zhonglinye/jun/ur5_lerobot_dataset/ur5_lerobot_dataset \
  --policy.path=lerobot/smolvla_base \
  --output_dir=outputs/smolvla_ur5_first \
  --job_name=smolvla_ur5_first \
  --policy.device=cuda \
  --policy.push_to_hub=false \
  --wandb.enable=true \
  --steps=30000 \
  --batch_size=32 \
  --rename_map='{"observation.images.base":"observation.images.camera1","observation.images.wrist":"observation.images.camera2"}' \
  --half_img_resolu=false \
  > train_log/test_smolvla_ur5_first.log 2>&1




2026年8月20日 23:44:41 执行了
bash launch_random.sh 42 0
Launched experiment: random_112_seed42
tmux session: random_112_s42
Output dir: /data/zhonglinye/jun/lerobot/personal/work2/duibi/random_42

Monitor with: tmux attach -t random_112_s42
Check logs: tail -f /data/zhonglinye/jun/lerobot/personal/work2/duibi/random_42/logs/random_112_seed42.log

以及

bash launch_uniform.sh 1
Launched experiment: uniform_112_seed42
tmux session: uniform_112_s42
Output dir: /data/zhonglinye/jun/lerobot/personal/work2/duibi/uniform_42

Monitor with: tmux attach -t uniform_112_s42
Check logs: tail -f /data/zhonglinye/jun/lerobot/personal/work2/duibi/uniform_42/logs/uniform_112_seed42.log


2026年8月21日14:50:42 修改代码支持resume
因为执行过程中出现了错误AttributeError: 'MetaworldEnv' object has no attribute 'obj_init_pos'，修改了代码以后尝试resume train
cd personal/work2/duibi/train_and_eval_scripts/
bash launch_random.sh 42 0
