2026年6月10日15时45分04秒开始
# 配置环境
conda create -y -n lb_local python=3.12
conda activate lb_local 
pip install -e ".[core_scripts]"
（# For robot workflows (recording, replaying, calibrate)）
pip install 'lerobot[feetech]'      
(feetech舵机)
conda install ffmpeg -c conda-forge
python -c "import torchcodec" 检查
最后检查python
import lerobot
lerobot.__path__
如果不是正确的目录，可能是PYTHONPATH的问题。
配置环境差不多结束。


# 本地数据上传到服务器
因为现在不知道服务器的代码到底从哪里寻找数据集的，所以先上传到指定位置。以后再copy

rsync -avz /home/qwe/.cache/huggingface/lerobot/ep10 zhonglinyue@10.10.16.18:/data/zhonglinye/jun/ep10
rsync 看的是源路径 (source) 有没有 /，目标路径 (destination) 的 / 基本没有影响（只要目标是目录）

假设本地是
local/
└── ep10/
    ├── a.txt
    └── b.txt

| 命令                              | 最终结果                                      | 说明                |
| ------------------------------- | ----------------------------------------- | ----------------- |
| `rsync local/ep10 server/`      | `server/ep10/a.txt`                       | **复制 ep10 整个目录**  |
| `rsync local/ep10/ server/`     | `server/a.txt`                            | **复制 ep10 里面的内容** |
| `rsync local/ep10 server/ep10`  | `server/ep10/a.txt`（如果 `server/ep10` 不存在） | 相当于把目录命名成 ep10    |
| `rsync local/ep10/ server/ep10` | `server/ep10/a.txt`                       | 把内容放进 ep10        |
| `rsync local/ep10 server/ep10/` | `server/ep10/ep10/a.txt`（如果 `server/ep10` 存在）     | 把内容放进 ep10        |

奇怪，怎么我服务器上没有ep10这个目录，rsync -avz /home/qwe/.cache/huggingface/lerobot/ep10 zhonglinyue@10.10.16.18:/data/zhonglinye/jun/ep10这个指令给我上传到 jun/ep10/ep10里面了



## 端口号
lerobot-find-port
找到主4从3
校准
lerobot-calibrate \
    --robot.type=so100_follower \
    --robot.port=/dev/ttyACM3 \
    --robot.id=jun_follower




## 最简单的joint
### teleoperate
lerobot-teleoperate \
    --robot.type=so100_follower \
    --robot.port=/dev/ttyACM0 \
    --robot.cameras="{ front: {type: opencv, index_or_path: 0, width: 640, height: 480, fps: 30}}" \
    --robot.id=start_new_heihei_2 \
    --teleop.type=so100_leader \
    --teleop.port=/dev/ttyACM1 \
    --teleop.id=start_new_leader_heihei \
    --display_data=false
可以运行！就是标定最好再标下

### record
lerobot-record \\
    --robot.type=so100_follower \\
    --robot.port=/dev/tty.usbmodem58760431541 \\
    --robot.cameras="{laptop: {type: opencv, index_or_path: 0, width: 640, height: 480, fps: 30}}" \\
    --robot.id=black \\
    --teleop.type=so100_leader \\
    --teleop.port=/dev/tty.usbmodem58760431551 \\
    --teleop.id=blue \\
    --dataset.repo_id=test/first \\
    --dataset.num_episodes=1 \\
    --dataset.single_task="Grab the cube" \\
    --dataset.streaming_encoding=true \\
    --dataset.encoder_threads=2 \\
    --display_data=true

## ee
先下载指定的文件夹，不然stl文件就不包含
github.com/TheRobotStudio/SO-ARM100/tree/main/Simulation/SO101
pip install 'lerobot[placo-dep]'
一个新的报错在常识import placo的时候出现了
ImportError: placo is installed but failed to import: liburdfdom_sensor.so.4.0: cannot open shared object file: No such file or directory
虽然有pr，但还是美发解决，所以
sudo apt update
sudo apt install liburdfdom-dev liburdfdom-sensor-dev
还是不行发issue了： github.com/huggingface/lerobot/issues/3755

有个人给出了解决方案
python -m pip install --force-reinstall \
  "cmeel-urdfdom==4.0.1" \
  "cmeel-tinyxml2==10.0.0"
执行以后import成功了


# 增加指定的模块
## 深度相机
以前看的一个pr有冲突



## 力
估计得我自己加，但是暂时用不上，所以先不管了。
现在要开始添加了，首先找到当时的仓库wowskin
说实话不知道过去的代码还能不能适配
git clone https://github.com/WowRobo-Robotics/WowSkin
cd WowSkin
pip install -r requirements.txt
pip install -e .

暂时生成了代码，然后尝试采集

记得安装pip install 'lerobot[intelrealsense]'

Try running `lerobot-find-port`

sudo chmod 666 /dev/ttyACM0
sudo chmod 666 /dev/ttyACM1
sudo chmod 666 /dev/ttyACM2

首先检查可视化
python WowSkin/wowskin_viz.py -p /dev/ttyACM1


lerobot-record \
    --robot.type=so100_follower \
    --robot.port=/dev/ttyACM0 \
    --robot.cameras="{wrist: {type: opencv, index_or_path: 0, width: 640, height: 480, fps: 30},top: {type: intelrealsense, serial_number_or_name: 806312060427, width: 640, height: 480, fps: 30, use_depth: False}}" \
    --robot.id=start_new_heihei_2 \
    --teleop.type=so100_leader \
    --teleop.port=/dev/ttyACM1 \
    --teleop.id=start_new_leader_heihei \
    --dataset.repo_id=test/second \
    --dataset.num_episodes=1 \
    --dataset.single_task="Grab the cube" \
    --dataset.streaming_encoding=true \
    --dataset.encoder_threads=2 \
    --display_data=true


git submodule add https://github.com/WowRobo-Robotics/WowSkin WowSkin

修改代码

lerobot-dataset-viz \
    --repo-id test/first \
    --root /home/qwe/.cache/huggingface/lerobot/test/first \
    --episode-index 0


修改代码，想可视化看一下force，prompt是
```
给lerobot_dataset_viz.py代码加一个开关，当外部指令为
lerobot-dataset-viz
--show-game-force true
--repo-id test/first
--root /home/qwe/.cache/huggingface/lerobot/force/test
--episode-index 0
的时候（默认值是false），会调用数据集中的force，并参考wowskin_viz.py里面的“以圆形面积代表磁场强度” 展示数据。修改的代码上要写注释
```


测试：
lerobot-dataset-viz \
--show-game-force true \
--repo-id test/first \
--root /home/qwe/.cache/huggingface/lerobot/test/first \
--episode-index 0

没有去掉baseline，那再重新采集，采集的时候顺便看viz的效果
用debug的指令采集

首先检查数据对不对，仿照wowskin.py的代码重新写了lerobot_record.py，现在的代码可以了
record保存->viz可视化（选择启发式窗口就能看到所有的）ok，接下来还需要：采集的时候能看到

record里面用display_data控制，对应
if display_data:
    log_rerun_data(
        observation=obs_processed, action=action_values, compress_images=display_compressed_images
    )
utils里面的代码没有加上force可视化相关

lerobot-edit-dataset \
    --repo_id ep10/valve \
    --operation.type delete_episodes \
    --operation.episode_indices "[2]"
删除一些不想要的，删除了以后它们会重新排序吗？




# 文档总结
## action_representation.mdx 
2个有效信息
1. 怎么计算ee
```python
from lerobot.model.kinematics import RobotKinematics
from lerobot.robots.so_follower.robot_kinematic_processor import (
    ForwardKinematicsJointsToEE,
    InverseKinematicsEEToJoints,
)

kinematics = RobotKinematics(
    urdf_path="./SO101/so101_new_calib.urdf",
    target_frame_name="gripper_frame_link",
    joint_names=["shoulder", "elbow", "wrist_pitch", "wrist_roll", "wrist_yaw"],
)

# Joints → EE (for observations: "where is my gripper?")
fk_step = ForwardKinematicsJointsToEE(kinematics=kinematics, motor_names=[...])

# EE → Joints (for actions: "move my gripper here")
ik_step = InverseKinematicsEEToJoints(kinematics=kinematics, motor_names=[...])
```
2. 转换delta

Enabling relative actions for the pi family (pi0, pi0.5, pi0_fast)

**Step 1**: Precompute relative action statistics for your dataset:

```bash
lerobot-edit-dataset \
    --repo_id your_dataset \
    --operation.type recompute_stats \
    --operation.relative_action true \
    --operation.chunk_size 50 \
    --operation.relative_exclude_joints "['gripper']"
```

**Step 2**: Train with relative actions enabled:

```bash
lerobot-train \
    --dataset.repo_id=your_dataset \
    --policy.type=pi0 \
    --policy.use_relative_actions=true \
    --policy.relative_exclude_joints='["gripper"]'
```

The `relative_exclude_joints` parameter specifies joints that should remain in absolute space. For example, gripper commands are typically binary (open/close) and don't benefit from relative encoding.


so，还没有直接将ee集成到代码里面


### 


# 新功能：支持tinyvla
本地只要增加模型配置的内容就可以了
测试的时候需要安装
pip install 'lerobot[async]' 

下载tinyvla模型除了safetensors的内容
rsync -av --progress --exclude='*.safetensors' zhonglinyue@10.10.16.18:/data/zhonglinye/jun/lerobot/outputs/ /home/qwe/jun/lerobot/outputs/


第一次运行得到设备信息
lerobot-find-port
sudo chmod 666 /dev/ttyACM1

lerobot-find-cameras opencv

第一次运行，好吧我训练的tinyvla没有换名字，小事小事
CUDA_VISIBLE_DEVICES=0 python -m lerobot.async_inference.robot_client_device_capture \
    --mode=capture \
    --save_path=./device_info.pkl \
    --server_address=10.10.16.18:8080 \
    --robot.type=so100_follower \
    --robot.port=/dev/ttyACM1 \
    --robot.id=start_new_heihei_2 \
    --robot.cameras="{ \
        wrist:{type: opencv, index_or_path: 6, width: 640, height: 480, fps: 30}, \
        top: {type: intelrealsense, serial_number_or_name: 806312060427, width: 640, height: 480, fps: 30, use_depth: False} \
    }" \
    --task="pick up the component and throw it away." \
    --policy_type=tinyvla \
    --pretrained_name_or_path=outputs/tinyvla_real_bushing/checkpoints/001000/pretrained_model \
    --policy_device=cuda \
    --actions_per_chunk=50 \
    --chunk_size_threshold=0 \
    --aggregate_fn_name=weighted_average \
    --debug_visualize_queue_size=True \
    --robot.use_degrees=True



后续运行

CUDA_VISIBLE_DEVICES=0 python -m lerobot.async_inference.robot_client_device_capture \
    --mode=replay \
    --save_path=./device_info.pkl \
    --server_address=10.10.16.18:8080 \
    --robot.type=so100_follower \
    --robot.port=/dev/ttyACM1 \
    --robot.id=start_new_heihei_2 \
    --robot.cameras="{ \
        wrist:{type: opencv, index_or_path: 6, width: 640, height: 480, fps: 30}, \
        top: {type: intelrealsense, serial_number_or_name: 806312060427, width: 640, height: 480, fps: 30, use_depth: False} \
    }" \
    --task="pick up the component and throw it away." \
    --policy_type=tinyvla \
    --pretrained_name_or_path=outputs/tinyvla_real_bushing/checkpoints/001000/pretrained_model \
    --policy_device=cuda \
    --actions_per_chunk=50 \
    --chunk_size_threshold=0 \
    --aggregate_fn_name=weighted_average \
    --debug_visualize_queue_size=True \
    --robot.use_degrees=True


# 新功能：half image resolution测试
服务器代码已经修改完了，本地需要识别+分辨率减半

示例
python -m lerobot.async_inference.robot_client \
    --robot.type=so100_follower \
    --robot.port=/dev/ttyACM0 \
    --robot.cameras="{camera1: {type: opencv, index_or_path: 0, width: 640, height: 480, fps: 30}}" \
    --robot.id=my_robot \
    --task="pick up the component" \
    --server_address=10.10.16.18:8080 \
    --policy_type=tinyvla \
    --pretrained_name_or_path=path/to/model \
    --policy_device=cuda \
    --actions_per_chunk=50 \
    --chunk_size_threshold=0 \
    --half_img_resolu=true


# 新功能：其他服务器推理
1. 有其他代码，但是直接python运行以后出来的是adb，不知道怎么使用
2. 增加一个get_max_relative_target.py代码，可以得到示范数据集里面的max_relative_target。
   - 使用方法：修改脚本中的 `repo_id` 和 `root` 为你的数据集路径，然后运行 `python get_max_relative_target.py`
   - 脚本会输出每个关节的最大单帧变化量，并生成可直接复制到 `launch.json` args 中的 `--robot.max_relative_target=...` 参数
   - 将输出的参数添加到 launch.json 对应配置的 `"args"` 数组中即可启用安全限制
3. 快速查看task：
python -c "import pandas as pd; print(pd.read_parquet('tasks.parquet'))"