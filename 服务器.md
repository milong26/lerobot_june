2026年6月10日15时46分42秒 开始
# 配置环境基础
conda create -y -n lb_server python=3.12
conda activate lb_server
pip install torch torchvision --index-url https://download.pytorch.org/whl/cu128
验证python -c "import torch; print(torch.cuda.is_available()); print(torch.cuda.get_device_name(0))"
安装服务器版本的
pip install -e ".[training]"
暂时差不多了，smolvla的模型以后再下载

想使用git遇到git-lfs filter-process: 1: git-lfs: not found
fatal: 远端意外挂断了
在不用sudo的时候安装git-lfs

git config --list | grep lfs 发现还是有的
filter.lfs.clean=git-lfs clean -- %f
filter.lfs.smudge=git-lfs smudge -- %f
filter.lfs.process=git-lfs filter-process
filter.lfs.required=true
lfs.repositoryformatversion=0

git config --show-origin --list | grep lfs找路径发现都是个人
file:/home/zhonglinyue/.gitconfig       filter.lfs.clean=git-lfs clean -- %f
file:/home/zhonglinyue/.gitconfig       filter.lfs.smudge=git-lfs smudge -- %f
file:/home/zhonglinyue/.gitconfig       filter.lfs.process=git-lfs filter-process
file:/home/zhonglinyue/.gitconfig       filter.lfs.required=true
file:.git/config        lfs.repositoryformatversion=0

删掉这些配置
git config --global --unset filter.lfs.clean
git config --global --unset filter.lfs.smudge
git config --global --unset filter.lfs.process
git config --global --unset filter.lfs.required

现在分支切过来了
git commit 的时候有个警告This repository is configured for Git LFS but 'git-lfs' was not found on your path. If you no longer wish to use Git LFS, remove this hook by deleting the 'post-commit' file in the hooks directory (set by 'core.hookspath'; usually '.git/hooks').
因为我不会上传大文件，所以直接去掉比较好
rm -f .git/hooks/post-commit
rm -f .git/hooks/pre-push
rm -f .git/hooks/pre-commit


# 仿真环境
2026年6月16日 今日任务是配置好仿真环境
提供了两个，meta-world，libero和还有hilserl

## meta-world
conda activate lb_server
pip install -e ".[metaworld]"

看起来安装好了，接下来做一个准备数据、训练act、测试的完整过程

`We provide a LeRobot-ready dataset for Meta-World MT50 on the HF Hub:` from metaworld.mdx

https://hf-mirror.com/datasets/lerobot/metaworld_mt50

下载数据集
尝试 
hf download --repo-type dataset lerobot/metaworld_mt50

（之前配置过应该是从hf-mirror下载的，所以不需要很久，但是要检查一下下载后的目录对不对）
没有运行完成，报了错httpx.ConnectTimeout: _ssl.c:993: The handshake operation timed out 网络问题真没办法
数据集到底是在
/data/zhonglinye/hfdata/lerobot/hub/datasets--lerobot--metaworld_mt50 这个目录是新增的，难道实际上运行lerobot代码的时候会想从这个目录找数据集？
还是
/data/zhonglinye/hfdata/hub/datasets--lerobot--metaworld_mt50
所以我删掉了rm -rf /data/zhonglinye/hfdata/hub/datasets--lerobot--metaworld_mt50
所以hf download的时候需要指定路径




训练，给的案例是smolvla，但是我想先用act试一下
nohup env CUDA_VISIBLE_DEVICES=0 lerobot-train \
  --policy.type=act \
  --dataset.repo_id=lerobot/metaworld_mt50 \
  --env.type=metaworld \
  --env.task=assembly-v3 \
  --output_dir=sim/outputs/mt_act_assembly \
  --job_name=act_assembly \
  --policy.device=cuda \
  --steps=20000 \
  --wandb.enable=true \
  > train_log/mt_act.log

    --dataset.root= \如果不行再指定目录

1. 环境配置好了吗？好了
2. 数据集下载全了吗？怎么还在下载？现在用的是hf-mirror吗？

怎么下载完以后就在训练了，打开wandb检查一下是不是真的在训练，还真的在训练，20k步大概需要6h
训练好了，得到了checkpoint，但是还需要eval，目前是有报错
```
mujoco.FatalError: an OpenGL platform library has not been loaded into this process
this most likely means that a valid OpenGL context has not been created
before mjr_makeContext was called
```
因为默认情况下 MuJoCo 会尝试GLFW / OpenGL (desktop context)
所以还得设置渲染方式，改成无头的才行。
1. 无头渲染怎么设置
   1. 强制 MuJoCo 使用 EGL，export MUJOCO_GL=egl
2. 在原来train的基础上




pi0-fast


pi0

# multi_gpu_training
确认加速
pip show accelerate
Name: accelerate
Version: 1.13.0

然后配置
accelerate config
先用这个配置
-------------------------------------------------------------------------------------------------------------------------------------------------------------In which compute environment are you running?
This machine                                                                                                                                                 
-------------------------------------------------------------------------------------------------------------------------------------------------------------Which type of machine are you using?                                                                                                                         
multi-GPU                                                                                                                                                    
How many different machines will you use (use more than 1 for multi-node training)? [1]:                                                                     
Should distributed operations be checked while running for errors? This can avoid timeout issues but will be slower. [yes/NO]: NO                            
Do you wish to optimize your script with torch dynamo?[yes/NO]:NO                                                                                            
Do you want to use DeepSpeed? [yes/NO]: NO                                                                                                                   
Do you want to use FullyShardedDataParallel? [yes/NO]: NO                                                                                                    
Do you want to use Megatron-LM ? [yes/NO]: NO                                                                                                                
How many GPU(s) should be used for distributed training? [1]:3                                                                                               
What GPU(s) (by id) should be used for training on this machine as a comma-separated list? [all]:0,1,2                                                       
Would you like to enable numa efficiency? (Currently only supported on NVIDIA hardware). [yes/NO]: NO                                                        
-------------------------------------------------------------------------------------------------------------------------------------------------------------Do you wish to use mixed precision?                                                                                                                          
fp16                


验证
accelerate env

使用
CUDA_VISIBLE_DEVICES=0,1,2 \
accelerate launch \
  --multi_gpu \
  --num_processes=3 \
  $(which lerobot-train) \



# pi0-fast

pi0-fast 检验能不能训练+推理
pip install -e ".[pi]"
准备FAST tokenizer，先用官方的lerobot/fast-action-tokenizer
如果要自己训练（？）不知道这个指令有没有用
```
lerobot-train-tokenizer \
    --repo_id "lerobot/metaworld_mt50" \
    --action_horizon 10 \
    --encoded_dims "0:4" \
    --vocab_size 1024 \
    --scale 10.0 \
    --normalization_mode QUANTILES \
    --output_dir "./metaworld_fast_tokenizer" \
    --push_to_hub \
    --hub_repo_id "yourname/metaworld-fast-tokenizer"
```
看能否跑通
lerobot-train \
  --policy.type=pi0_fast \
  --dataset.repo_id=lerobot/metaworld_mt50 \
  --env.type=metaworld \
  --env.task=assembly-v3 \
  --output_dir=sim/outputs/mt_pi0fast_assembly \
  --job_name=pi0fast_mt_assembly \
  --policy.device=cuda \
  --steps=1000 \
  --batch_size=4 \
  --policy.dtype=bfloat16 \
  --policy.gradient_checkpointing=true \
  --policy.chunk_size=10 \
  --policy.n_action_steps=10 \
  --policy.max_action_tokens=256 \
  --policy.action_tokenizer_name=lerobot/fast-action-tokenizer
它应该会执行下载tokenizer的操作，直接下载失败了。还是必须自己下载。smolvla下载在哪里？/data/zhonglinye/hfdata/huggingface/hub这个目录吗？
离线下载，需要下载两个：分词：google/paligemma-3b-pt-224
hf download --repo-type model lerobot/fast-action-tokenizer
hf download --repo-type model google/paligemma-3b-pt-224 没法执行，报错说需要权限

hf auth whoami
说没登录
然后登录hf auth login
需要从huggingface上搞令牌

还是需要权限，之前的token可能不够？新建了一个功能是read的，终于可以了，之前好像选的fine-grained
忘记开加速训练了，单gpu果然不行。
试一下用1，2能不能
CUDA_VISIBLE_DEVICES=1,2 \
accelerate launch \
  --multi_gpu \
  --num_processes=2 \
  $(which lerobot-train) \
  --policy.type=pi0_fast \
  --dataset.repo_id=lerobot/metaworld_mt50 \
  --env.type=metaworld \
  --env.task=assembly-v3 \
  --output_dir=sim/outputs/mt_pi0fast_assembly \
  --job_name=pi0fast_mt_assembly \
  --policy.device=cuda \
  --steps=1000 \
  --batch_size=1 \
  --policy.dtype=bfloat16 \
  --policy.gradient_checkpointing=true \
  --policy.chunk_size=10 \
  --policy.n_action_steps=10 \
  --policy.max_action_tokens=256 \
  --policy.action_tokenizer_name=lerobot/fast-action-tokenizer

（目的只是测试能不能运行lerobot/pi0fast-base）



ok啊现在是不知道到底是看hub的还是hugginface/hub里面的
好吧，应该就是hub里面的了，我知道了



# smolvla
## 环境配置
1. conda activate lb_server
2. pip install -e ".[smolvla]"

模型需要哪些？
1. vlm骨干：HuggingFaceTB/SmolVLM2-500M-Video-Instruct
能否直接指定一个位置
hf download HuggingFaceTB/SmolVLM2-500M-Video-Instruct --resume-download

大小5.58G，需要一点时间。如果中途报错停下来，就
hf download HuggingFaceTB/SmolVLM2-500M-Video-Instruct

需要验证
1. 不指定位置的话，直接下载到哪里？/data/zhonglinye/hfdata/hub/models--HuggingFaceTB--SmolVLM2-500M-Video-Instruct
2. 默认位置，代码能否直接找到？
测试的话，就直接用之前的数据集。
=======
待会儿继续




2. 分词



# 新增-tinyvla
可以使用tinyvla这个policy，ai写完代码了，还没检查能否训练。
准备policy
hf download lesjie/Llava-Pythia-400M 
暂时先下载下来，放在默认目录。

pip install diffusers
pip install peft==0.18.1
===
等下载完成以后执行：

CUDA_VISIBLE_DEVICES=0 lerobot-train \
  --dataset.repo_id=ep10/bushing \
  --dataset.root=/data/zhonglinye/jun/ep10/ep10/bushing \
  --policy.type=tinyvla \
  --policy.model_name_or_path=lesjie/Llava-Pythia-400M  \
  --policy.action_head_type=droid_diffusion \
  --policy.action_dim=10 \
  --policy.state_dim=7 \
  --policy.chunk_size=16 \
  --policy.n_action_steps=16 \
  --output_dir=outputs/tinyvla_test