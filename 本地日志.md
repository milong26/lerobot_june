2026年6月10日15时45分04秒开始
# 配置环境
conda create -y -n lb_local python=3.12
conda activate lb_local 
pip install -e ".[core_scripts]"
（# For robot workflows (recording, replaying, calibrate)）
pip install 'lerobot[feetech]'      
(feetech舵机)
conda install ffmpeg -c conda-forge
python -c "import torchcodec" 检查
最后检查python
import lerobot
lerobot.__path__
如果不是正确的目录，可能是PYTHONPATH的问题。
配置环境差不多结束。

# 测试功能
## 端口号
lerobot-find-port
找到主4从3
校准
lerobot-calibrate \
    --robot.type=so100_follower \
    --robot.port=/dev/ttyACM3 \
    --robot.id=jun_follower




## 最简单的joint
### teleoperate
lerobot-teleoperate \
    --robot.type=so100_follower \
    --robot.port=/dev/ttyACM3 \
    --robot.cameras="{ front: {type: opencv, index_or_path: 12, width: 640, height: 480, fps: 30}}" \
    --robot.id=start_new_heihei_2 \
    --teleop.type=so100_leader \
    --teleop.port=/dev/ttyACM4 \
    --teleop.id=start_new_leader_heihei \
    --display_data=true
可以运行！就是标定最好再标下

### record
lerobot-record \\
    --robot.type=so100_follower \\
    --robot.port=/dev/tty.usbmodem58760431541 \\
    --robot.cameras="{laptop: {type: opencv, index_or_path: 0, width: 640, height: 480, fps: 30}}" \\
    --robot.id=black \\
    --teleop.type=so100_leader \\
    --teleop.port=/dev/tty.usbmodem58760431551 \\
    --teleop.id=blue \\
    --dataset.repo_id=test/first \\
    --dataset.num_episodes=1 \\
    --dataset.single_task="Grab the cube" \\
    --dataset.streaming_encoding=true \\
    --dataset.encoder_threads=2 \\
    --display_data=true

## ee
先下载指定的文件夹，不然stl文件就不包含
github.com/TheRobotStudio/SO-ARM100/tree/main/Simulation/SO101
pip install 'lerobot[placo-dep]'
一个新的报错在常识import placo的时候出现了
ImportError: placo is installed but failed to import: liburdfdom_sensor.so.4.0: cannot open shared object file: No such file or directory
虽然有pr，但还是美发解决，所以
sudo apt update
sudo apt install liburdfdom-dev liburdfdom-sensor-dev
还是不行发issue了 github.com/huggingface/lerobot/issues/3755
先用一般的joint试试


# 增加指定的模块
## 深度相机

## 力


# 文档总结
（不是本地代码独有的）
文档有点多，这里总结点：
- action_representation.mdx 